USER_LOGIN="${USER}"
DOMAIN_NAME="${DOMAIN_NAME}"

NGINX_HOST_PORT=${NGINX_HOST_PORT}
NGINX_LISTEN_PORT=${NGINX_LISTEN_PORT}
NGINX_PHP_SERVICE=${NGINX_PHP_SERVICE}
NGINX_PHP_SERVICE_PORT=${NGINX_PHP_SERVICE_PORT}
WEB_DATA="${WEB_DATA}"

KEY_NAME="${KEY_SECRET_NAME}"
KEY_PATH="${KEY_PATH}"
CERT_NAME="${CERT_SECRET_NAME}"
CERT_PATH="${CERT_PATH}"

PHPFPM_HOST="${WP_CONTAINER_NAME}"
PHPFPM_LISTEN_PORT=${PHPFPM_LISTEN_PORT}
