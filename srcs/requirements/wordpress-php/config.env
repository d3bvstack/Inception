USER_LOGIN="${USER_LOGIN}"
DOMAIN_NAME="${DOMAIN_NAME}"
SITE_TITLE="${SITE_TITLE}"

# Root domain used by wp-cli for installs.
ROOT_DOMAIN="${ROOT_DOMAIN}"

NGINX_HOST="${NGINX_CONTAINER_NAME}"
NGINX_LISTEN_PORT=${NGINX_PHP_LISTEN_PORT}

DB_NAME="${WP_DB_NAME}"
DB_ADMIN="${WP_DB_ADMIN}"
DB_HOST="${MDB_CONTAINER_NAME}"
DB_PORT=${MDB_ENGINE_PORT}

WP_VERSION="${WP_VERSION}"
WP_WEBROOT="${WP_WEBROOT}"

WP_USER_ROLE="${WP_USER_ROLE}"

PHPFPM_LISTEN_PORT=${PHPFPM_LISTEN_PORT}
PHP_FPM_VERSION=${PHP_FPM_VERSION}
